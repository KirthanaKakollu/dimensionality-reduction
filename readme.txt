The folder contains three directories named after the datasets.

Each folder has a 'main' file which should be executed. The code will run over 20 epochs of partitioning the data and carries out the feature selection and classification tasks.

In the end, the code plots the error percentages of various procedures and computes the mean and standard deviation statistics.